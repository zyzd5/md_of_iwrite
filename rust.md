* `use` means `using` in cc

* `loop` for infinite loop

* `match` 
```rust
match guess {
    Ok()
}
```

* String
```rust
.trim()         // trim() 去除字符串开头和结尾的空白字符
.parse()        // parse() 将字符串转换为其他类型, u32 来指定类型
.expect("type a number");
```
